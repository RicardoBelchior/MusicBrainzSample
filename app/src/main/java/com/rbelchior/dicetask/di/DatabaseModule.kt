package com.rbelchior.dicetask.di

import org.koin.dsl.module

internal val databaseModule = module {
//    single { WalletDatabase(get()).walletDatabaseQueries }
//    single<WalletLocalDataSource> { WalletLocalDataSourceImpl(get()) }
}
